<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/tab_home"
        android:enabled="true"
        android:title="@string/tab_home"
        android:icon="@drawable/ic_home"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/tab_ticker"
        android:enabled="true"
        android:title="@string/tab_ticker"
        android:icon="@drawable/ic_ticker"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/tab_trade"
        android:enabled="true"
        android:title="@string/tab_trade"
        android:icon="@drawable/ic_trade"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/tab_asset"
        android:enabled="true"
        android:title="@string/tab_asset"
        android:icon="@drawable/ic_wallet"
        app:showAsAction="ifRoom" />

</menu>